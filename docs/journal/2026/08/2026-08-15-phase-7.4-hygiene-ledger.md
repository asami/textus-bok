# Phase 7.4 Hygiene Ledger

Phase 7.4 closed on 2026-08-15 with no actionable implementation or release
finding. The following inherited or external-tool items remain explicitly
outside the Phase 7.4 runtime closure.

## P7.4-HY-01: External CAR Descriptor Helper

Normal CAR lint reports `car-descriptor.contract` because the external Python
helper expects legacy root `name` and `component` fields. Integrated Cozy lint
accepts the generated schema-v3 descriptor and reports
`CAR_COMPONENT_IDENTITY_CANONICAL` for `org.simplemodeling.textus/Bok`.
This is the existing `P7.1-HY-01` helper mismatch, not a generated CAR defect.

## P7.4-HY-02: Release and ABI Baseline

`abi.baseline.missing` and `build.sbt-cozy-latest` remain release-readiness
work. Phase 7.4 intentionally validates the current SNAPSHOT development tree
and does not publish or create an ABI release baseline.

## P7.4-HY-03: Nominal String Wrappers

The existing `cml.datatype.nominal-string-wrapper` warnings remain tracked by
`P7-HY-03` / `HYG-P7.2-001`. They do not change profile-selection resolution,
attribution, or cross-surface failure behavior and were not admitted into this
closure Step.

These items must not be described as completed. A future explicitly approved
release-readiness or hygiene scope may take ownership; Phase 7.4 does not imply
or start that successor work.
